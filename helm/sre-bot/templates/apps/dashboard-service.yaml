apiVersion: v1
kind: Service
metadata:
    name: {{ .Values.dashboard.appName }}
    namespace: {{ .Values.ns.app_namespace }}
spec:
    selector:
        app: {{ .Values.dashboard.appName }}
    ports:
        - name: container
          port: {{ .Values.dashboard.listenPort }}
          targetPort: {{ .Values.dashboard.listenPort }}