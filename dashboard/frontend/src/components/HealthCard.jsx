// This is a React "functional component" — a function that returns HTML-like syntax called JSX
// Every component must export a default function so other files can import it

export default function HealthCard() {
  // For now just render a placeholder div
  // We'll fill this in after App.jsx is working
  return <div>HealthCard</div>
}